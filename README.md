A ReactJS application to generate strong passwords. To build simply run `npm install` and `npm run build`. At which point all you should need to do is point the documentent root to the build directory.